---
/**
 * ScrollProgress - Astro Component with React interactivity
 * Displays a progress bar at the top of the page that fills as user scrolls
 * Uses framer-motion for smooth spring animations
 */

import ScrollProgressReact from './ScrollProgressReact';
---

<ScrollProgressReact client:load />
