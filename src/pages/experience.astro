---
import type { APIRoute } from 'astro';

export const GET: APIRoute = (): Response => {
  return new Response(null, {
    status: 301,
    headers: {
      Location: '/#experience',
    },
  });
};
---
