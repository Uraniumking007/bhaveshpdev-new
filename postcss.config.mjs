/** @type {import('postcss-load-config').Config} */
export default {
  plugins: {
    // Tailwind CSS v4 uses CSS-based imports (@import "tailwindcss")
    // No PostCSS plugin needed when using the CSS import approach
  },
};
